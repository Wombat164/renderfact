renders/
*.png
*.pdf
*.docx
*.svg
__pycache__/
.pytest_cache/
.fonts/
node_modules/
*.log
.renderfact-copy-paste-prompt.txt
*.egg-info/

# brand assets are deliberately committed (README logo + social preview)
!assets/brand/
!assets/brand/**

# template-pack exemplars are deliberately committed (identity-free: rendered with PROVENANCE=off)
!templates/renders/
!templates/renders/**

# demo exemplars are deliberately committed (the showcase must be visible without a local build)
!demo/renders/
!demo/renders/**
dist/
*.egg-info/
build/

# D17 local model config (secrets are env-only; the file is per-user)
renderfact-models.toml

# demo render byproducts (generated by demo/render-demo.sh)
demo/renders/*.pdf
demo/renders/*.docx
.venv/
